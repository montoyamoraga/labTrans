

void configurarTitulo() {
  textSize(32);
  textAlign(CENTER, CENTER);
}

void configurarFecha() {
  textSize(20);
  textAlign(CENTER, CENTER);
}

void configurarMetadata() {
  textSize(20);
  textAlign(CENTER, CENTER);
}
